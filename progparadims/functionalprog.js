 function hello() {
     hello.language='English';
     console.log(hello.language);
    console.log('Howdie');
}
//FirstClass functions -This mean that with functions you can do everything you can do with other types such as assigning them to variables. some of its properties are: are an instance of the Object type-have propeties and a link back to its constructor method-can be stored in a variable-can be passed as a parameter to another function
